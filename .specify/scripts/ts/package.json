{
  "name": "@tdk/tdk",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "detect-config": "bun src/commands/detect-config.ts",
    "manifest": "bun src/commands/manifest/compute.ts",
    "migrate": "bash ../bash/migrate-yaml-to-json.sh",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "commander": "^13.1.0",
    "yaml": "^2.5.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0",
    "typescript": "^5.5.0"
  }
}
