---
paths:
  - "src/api/**/*.ts"
description: "API design patterns and endpoint conventions"
inject: reference
---

This is a long reference document. Only the path and description are injected at runtime.
The agent can read the full file if needed.
